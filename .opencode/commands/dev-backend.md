---
description: Arranca el backend NestJS en modo desarrollo (puerto 3000)
agent: nestjs-dev
---

Arranca el backend NestJS en modo desarrollo ejecutando `make dev` desde el directorio `backend/`.

Muestra la salida del servidor y confirma que esta escuchando en el puerto 3000.

Si hay errores de compilacion, analiza el error y sugiere una solucion.
