---
description: Arranca el frontend Angular en modo desarrollo (puerto 4200)
agent: angular-dev
---

Arranca el frontend Angular en modo desarrollo ejecutando `npm start` desde el directorio `frontend/`.

Muestra la salida del servidor y confirma que esta sirviendo en el puerto 4200.

Si hay errores de compilacion, analiza el error y sugiere una solucion.
